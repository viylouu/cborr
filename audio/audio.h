#ifndef AUDIO_H
#define AUDIO_H

/*typedef struct {
    char filePath[256];
    char fileName[256]; //name
    float vol;
    int loop;  
    int musicFile;  //specifies if its a music file or not
} cBAudioFile;*/

int audio_init();

#endif 
